import { DriverOnboardingStep } from "@/components/driver/onboarding/DriverOnboardingStep";

/* Driver onboarding step six: Stripe-hosted payout onboarding. */
export default function DriverStripeOnboardingPage() {
  return <DriverOnboardingStep step="stripe" />;
}
