import { DriverOnboardingStep } from "@/components/driver/onboarding/DriverOnboardingStep";

/* Driver onboarding step two: verified home address and service area. */
export default function DriverAddressServiceAreaPage() {
  return <DriverOnboardingStep step="address-service-area" />;
}
